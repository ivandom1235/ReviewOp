This one is **not a strong run overall**. It is mainly a **tiny diagnostic run for hard abstain-heavy cases**.

## Overall verdict

**Score: 6.0/10**

It is very similar in character to your previous tiny stress-test run:

* useful for checking **abstain + ambiguity + H3 behavior**
* not useful as a **benchmark-quality artifact**

## What is good

### 1. Strong selective-inference stress profile

This is the main value of the run.

* **abstain_acceptable_rate:** **0.5789**
* **multi-gold-label rate:** **0.6842**
* **average gold interpretations:** **3.53**
* **benchmark hardness:** all exported rows are **H3**

So this run is good for testing whether the pipeline can produce:

* abstain-worthy rows
* high-ambiguity rows
* very hard implicit cases

### 2. Grouped leakage is clean

* train–val overlap: **0**
* train–test overlap: **0**
* val–test overlap: **0**

### 3. Evidence grounding is strong

* **grounded_evidence_rate:** **1.0**
* **grounded_prediction_rate:** **0.96**
* **explicit_in_implicit_rate:** **0.0**
* **boundary false positives:** **0**

That part is solid.

---

## What is weak

### 1. Benchmark is still extremely small

* sampled rows: **100**
* exported benchmark rows: **19**
* deferred review rows: **81**

So this is still far too small to judge real benchmark quality.

### 2. Split structure is basically unusable

* train: **18**
* val: **1**
* test: **0**

That means there is effectively **no usable test split**.

### 3. Train export collapsed again

This is the biggest practical failure.

* **train_export:** **0**
* **rows_before_targeting:** **0**
* **rows_after_targeting:** **0**

So this run cannot support a trainable artifact.

### 4. Telecom is missing in the exported benchmark

Source sample had telecom:

* electronics: **26**
* restaurant: **67**
* telecom: **7**

But exported benchmark has:

* electronics: **8**
* restaurant: **11**
* telecom: **0**

So domain coverage still fails at export level.

### 5. Implicit purity is still very low

* **implicit_purity_rate:** **0.1055**

### 6. Ontology compatibility is weak

* **ontology_compatibility_rate:** **0.6675**

### 7. Duplicate logical row rate is extremely high

* **duplicate_logical_row_rate:** **0.81**

That is a major warning sign. In such a tiny export, the slice is too repetitive to trust as a benchmark proxy.

---

## Promotion / quality status

This run is still blocked for real reasons:

* **debug_artifacts**
* **strict challenge too low**
* **missing core V1 domain family in exported benchmark**
* **low implicit purity**
* **low ontology compatibility**
* **worst-domain regression**

So this is not close to a clean release artifact.

---

## Compared with your previous tiny run

It is basically the same type of run:

* high abstain
* high ambiguity
* hard H3 cases
* clean grouped splits
* tiny export
* no train artifact
* telecom disappears at export time

So this is **not a clear improvement**. It is another **useful stress probe**, not a benchmark milestone.

## Bottom line

For a slow laptop small-sample run, this is **fine as a diagnostic checkpoint**.

It proves the pipeline can still generate:

* abstain-heavy rows
* high-ambiguity rows
* hard H3 rows
* clean evidence and no explicit contamination

But it is **not good as a benchmark artifact**, because:

* export is only **19 rows**
* test split is **0**
* train export is **0**
* telecom disappears
* purity is very low

## One-line summary

**Good tiny stress-test for abstain-heavy hard cases, but not a meaningful benchmark-quality run.**
